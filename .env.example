DISCORD_TOKEN=your-bot-token
DISCORD_CLIENT_ID=your-application-client-id
DISCORD_GUILD_ID=optional-guild-id-for-faster-command-deploy
DATABASE_URL=postgresql://postgres:password@localhost:5432/keking
POSTGRES_DB=keking # optional, for separate database images in docker-compose
POSTGRES_USER=postgres # optional, for separate database images in docker-compose
POSTGRES_PASSWORD=postgres # optional, for separate database images in docker-compose
YOUTUBE_COOKIES=youtube-cookies-string